import numpy as np
import torch
from torch.utils.data import Dataset

class BuildDataset(Dataset):
    def __init__(self):
        return None
    def __len__(self):
        return None
    def __getitem__(self, idx: int) -> dict:
        return None
